{
  "name": "media-intake-capstone-showcase",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:local": "cross-env SINGLEFILE=1 VITE_BASE=./ tsc --noEmit && cross-env SINGLEFILE=1 VITE_BASE=./ vite build && node scripts/copy-offline.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "cross-env": "^10.1.0",
    "typescript": "~5.7.2",
    "vite": "^6.0.3",
    "vite-plugin-singlefile": "^2.3.2"
  }
}
